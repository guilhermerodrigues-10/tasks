version: '3.8'

services:
  # Backend API
  backend:
    image: gulenda/flowstate-backend:latest
    environment:
      - DATABASE_URL=postgresql://postgres.ncbmjkhoplgyfgxeqhmo:QKuZg482wKvhWfgN@aws-1-us-east-1.pooler.supabase.com:6543/postgres?pgbouncer=true
      - JWT_SECRET=flowstate-production-secret-change-this-in-production
      - PORT=3001
    networks:
      - app-network
    deploy:
      replicas: 1
      placement:
        constraints:
          - node.role == manager

  # Frontend (Nginx + React)
  frontend:
    image: gulenda/flowstate:latest
    ports:
      - "3000:80"
    environment:
      - NODE_ENV=production
    networks:
      - app-network
    deploy:
      replicas: 1
      placement:
        constraints:
          - node.role == manager

networks:
  app-network:
    driver: overlay
