version: '3.8'

services:
  flowstate:
    image: gulenda/flowstate:latest
    ports:
      - "3000:80"
    environment:
      - NODE_ENV=production
    deploy:
      replicas: 1
      placement:
        constraints:
          - node.role == manager
